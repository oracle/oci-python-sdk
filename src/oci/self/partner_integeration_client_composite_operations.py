# coding: utf-8
# Copyright (c) 2016, 2026, Oracle and/or its affiliates.  All rights reserved.
# This software is dual-licensed to you under the Universal Permissive License (UPL) 1.0 as shown at https://oss.oracle.com/licenses/upl or Apache License 2.0 as shown at http://www.apache.org/licenses/LICENSE-2.0. You may choose either license.

# NOTE: This class is auto generated by OracleSDKGenerator. DO NOT EDIT. API Version: 20260129

import oci  # noqa: F401
from oci.util import WAIT_RESOURCE_NOT_FOUND  # noqa: F401


class PartnerIntegerationClientCompositeOperations(object):
    """
    This class provides a wrapper around :py:class:`~oci.self.PartnerIntegerationClient` and offers convenience methods
    for operations that would otherwise need to be chained together. For example, instead of performing an action
    on a resource (e.g. launching an instance, creating a load balancer) and then using a waiter to wait for the resource
    to enter a given state, you can call a single method in this class to accomplish the same functionality
    """

    def __init__(self, client, **kwargs):
        """
        Creates a new PartnerIntegerationClientCompositeOperations object

        :param PartnerIntegerationClient client:
            The service client which will be wrapped by this object
        """
        self.client = client

    def submit_subscription_usage_batch_and_wait_for_state(self, compartment_id, submit_subscription_usage_batch_details, wait_for_states=[], operation_kwargs={}, waiter_kwargs={}):
        """
        Calls :py:func:`~oci.self.PartnerIntegerationClient.submit_subscription_usage_batch` and waits for the :py:class:`~oci.self.models.WorkRequest`
        to enter the given state(s).

        :param str compartment_id: (required)
            The `OCID`__ of the compartment associated with the usage records request.

            __ https://docs.oracle.com/iaas/Content/General/Concepts/identifiers.htm

        :param stream submit_subscription_usage_batch_details: (required)
            UTF-8 CSV file with no more than 10,000 usage records and a maximum size of 50 MB.
            Required columns are `MarketplaceOfferId`, `Id`, `Amount`, `CurrencyCode`,
            `UsageStartTime`, `UsageEndTime`, and `UsageDimensionName`.
            Optional columns are `ConsumedQuantity`, `CustomerTenancyId`,
            `BillingIdentifier`, `ProductSku`, `UnitOfMeasure`, `UnitPrice`,
            `ContractDuration`, and `AdditionalMetadata`.
            When provided in CSV, `AdditionalMetadata` must be a JSON array of `ExtendedMetadata`
            objects. `MarketplaceOfferId` must be a subscription or private offer OCID.

        :param list[str] wait_for_states:
            An array of states to wait on. These should be valid values for :py:attr:`~oci.self.models.WorkRequest.status`

        :param dict operation_kwargs:
            A dictionary of keyword arguments to pass to :py:func:`~oci.self.PartnerIntegerationClient.submit_subscription_usage_batch`

        :param dict waiter_kwargs:
            A dictionary of keyword arguments to pass to the :py:func:`oci.wait_until` function. For example, you could pass ``max_interval_seconds`` or ``max_interval_seconds``
            as dictionary keys to modify how long the waiter function will wait between retries and the maximum amount of time it will wait
        """
        operation_result = self.client.submit_subscription_usage_batch(compartment_id, submit_subscription_usage_batch_details, **operation_kwargs)
        if not wait_for_states:
            return operation_result
        lowered_wait_for_states = [w.lower() for w in wait_for_states]
        if 'opc-work-request-id' not in operation_result.headers:
            return operation_result
        wait_for_resource_id = operation_result.headers['opc-work-request-id']

        try:
            waiter_result = oci.wait_until(
                self.client,
                self.client.get_work_request(wait_for_resource_id),
                evaluate_response=lambda r: getattr(r.data, 'status') and getattr(r.data, 'status').lower() in lowered_wait_for_states,
                **waiter_kwargs
            )
            result_to_return = waiter_result

            return result_to_return
        except Exception as e:
            raise oci.exceptions.CompositeOperationError(partial_results=[operation_result], cause=e)
