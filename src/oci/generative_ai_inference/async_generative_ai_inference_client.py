# coding: utf-8
# Copyright (c) 2016, 2026, Oracle and/or its affiliates.  All rights reserved.
# This software is dual-licensed to you under the Universal Permissive License (UPL) 1.0 as shown at https://oss.oracle.com/licenses/upl or Apache License 2.0 as shown at http://www.apache.org/licenses/LICENSE-2.0. You may choose either license.

# NOTE: This class is auto generated by OracleSDKGenerator. DO NOT EDIT. API Version: 20231130

from __future__ import absolute_import

from oci._vendor import six

from oci import regions, retry, circuit_breaker
from oci.async_base_client import AsyncBaseClient
from oci.config import get_config_value_or_default, validate_config
from oci.signer import Signer
from oci.util import Sentinel, get_signer_from_authentication_type, AUTHENTICATION_TYPE_FIELD_NAME
from oci.exceptions import InvalidDeveloperToolConfiguration
from oci.developer_tool_configuration import OCI_SDK_ENABLED_SERVICES_SET
from .models import generative_ai_inference_type_mapping
missing = Sentinel("Missing")


class AsyncGenerativeAiInferenceClient(object):
    """
    Asynchronous client for GenerativeAiInferenceClient.

    OCI Generative AI is a fully managed service that provides a set of state-of-the-art, customizable large language models (LLMs) that cover a wide range of use cases for text generation, summarization, and text embeddings.

    Use the Generative AI service inference API to access your custom model endpoints, or to try the out-of-the-box models to [chat](#/EN/generative-ai-inference/latest/ChatResult/Chat), [generate text](#/EN/generative-ai-inference/latest/GenerateTextResult/GenerateText), [summarize](#/EN/generative-ai-inference/latest/SummarizeTextResult/SummarizeText), and [create text embeddings](#/EN/generative-ai-inference/latest/EmbedTextResult/EmbedText).

    To use a Generative AI custom model for inference, you must first create an endpoint for that model. Use the [Generative AI service management API](#/EN/generative-ai/latest/) to [create a custom model](#/EN/generative-ai/latest/Model/) by fine-tuning an out-of-the-box model, or a previous version of a custom model, using your own data. Fine-tune the custom model on a [fine-tuning dedicated AI cluster](#/EN/generative-ai/latest/DedicatedAiCluster/). Then, create a [hosting dedicated AI cluster](#/EN/generative-ai/latest/DedicatedAiCluster/) with an [endpoint](#/en/generative-ai/latest/Endpoint/) to host your custom model. For resource management in the Generative AI service, use the [Generative AI service management API](#/EN/generative-ai/latest/).

    To learn more about the service, see the [Generative AI documentation](/iaas/Content/generative-ai/home.htm).

    **Important:** The IP addresses behind each DNS endpoint might change over time. Always use the DNS hostname listed under the following **API Endpoints** section and avoid using hard-coded fixed IP addresses.
    """

    __slots__ = ('async_base_client',)

    def __init__(self, config, **kwargs):
        """Creates a new asynchronous service client.

        ``circuit_breaker_strategy`` and ``circuit_breaker_callback`` use the
        same OCI circuit-breaker implementation and defaults as the synchronous
        client.
        """
        if not OCI_SDK_ENABLED_SERVICES_SET.is_service_enabled("generative_ai_inference"):
            raise InvalidDeveloperToolConfiguration("The Developer Tool Configuration has disabled this service, this behavior is controlled by OCI_SDK_ENABLED_SERVICES_SET variable. Please check if your local developer-tool-configuration file configured the service you're targeting or contact the cloud provider on the availability of this service")

        validate_config(config, signer=kwargs.get('signer'))
        if 'signer' in kwargs:
            signer = kwargs['signer']
        elif AUTHENTICATION_TYPE_FIELD_NAME in config:
            signer = get_signer_from_authentication_type(config)
        else:
            signer = Signer(
                tenancy=config["tenancy"],
                user=config["user"],
                fingerprint=config["fingerprint"],
                private_key_file_location=config.get("key_file"),
                pass_phrase=get_config_value_or_default(config, "pass_phrase"),
                private_key_content=config.get("key_content")
            )

        service_endpoint = kwargs.get('service_endpoint')
        if not service_endpoint:
            region_to_use = None
            if 'region' in config and config['region']:
                region_to_use = config.get("region")
            elif hasattr(signer, 'region'):
                region_to_use = signer.region

            service_endpoint = regions.endpoint_for(
                "generative_ai_inference",
                region=region_to_use,
                service_endpoint_template="https://inference.generativeai.{region}.{dualStack?ds.:}oci.{secondLevelDomain}",
                endpoint_service_name="inference.generativeai"
            )

        base_client_init_kwargs = {
            'service_endpoint': service_endpoint,
            'base_path': '/20231130',
            'service_uses_dualstack_endpoints_by_default': False,
            'client_level_dualstack_endpoints_enabled': kwargs.get('client_level_dualstack_endpoints_enabled'),
            'skip_deserialization': kwargs.get('skip_deserialization', False),
            'retry_strategy': kwargs.get('retry_strategy'),
            'circuit_breaker_strategy': kwargs.get('circuit_breaker_strategy', circuit_breaker.GLOBAL_CIRCUIT_BREAKER_STRATEGY),
            'circuit_breaker_callback': kwargs.get('circuit_breaker_callback')
        }
        if 'timeout' in kwargs:
            base_client_init_kwargs['timeout'] = kwargs.get('timeout')
        if base_client_init_kwargs.get('circuit_breaker_strategy') is None:
            base_client_init_kwargs['circuit_breaker_strategy'] = circuit_breaker.DEFAULT_CIRCUIT_BREAKER_STRATEGY

        self.async_base_client = AsyncBaseClient(
            "generative_ai_inference",
            config,
            signer,
            generative_ai_inference_type_mapping,
            **base_client_init_kwargs
        )

    async def apply_guardrails(self, apply_guardrails_details, **kwargs):
        """
        Applies guardrails to the input content, including content moderation, PII detection, and prompt injection protection.
        Case 1: Use `input` when the customer wants simple single-text moderation. Existing
        customers can continue to use this field without changing their current integration.
        Case 2: Use `multimodalInput` when the customer wants moderation over text, image, or a
        combination of both.
        `multimodalInput` supports a single text item, an array of text items only, an array of
        images only, or a mixed ordered combination of text and image items.
        Clients may provide `input`, `multimodalInput`, or both. At least one of these fields must
        be provided. If both `input` and `multimodalInput` are provided, the service will process
        `input` and discard `multimodalInput`.


        :param oci.generative_ai_inference.models.ApplyGuardrailsDetails apply_guardrails_details: (required)
            Details for applying guardrails to the input content.

        :param str opc_retry_token: (optional)
            A token that uniquely identifies a request so it can be retried in case of a timeout or
            server error without risk of executing that same action again. Retry tokens expire after 24
            hours, but can be invalidated before that, in case of conflicting operations. For example, if a resource is deleted and purged from the system, then a retry of the original creation request
            is rejected.

        :param str opc_request_id: (optional)
            The client request ID for tracing.

        :param obj retry_strategy: (optional)
            A retry strategy to apply to this specific operation/call. This will override any retry strategy set at the client-level.

        :param bool allow_control_chars: (optional)
            allow_control_chars is a boolean to indicate whether or not this request should allow control characters in the response object.

        :param bool enable_strict_url_encoding: (optional)
            enable_strict_url_encoding is a boolean to indicate whether or not this request should enable strict url encoding in path params of a request.

        :return: A :class:`~oci.response.Response` object with data of type ApplyGuardrailsResult
        :rtype: :class:`~oci.response.Response`
        """
        required_arguments = []
        resource_path = "/actions/applyGuardrails"
        method = "POST"
        operation_name = "apply_guardrails"
        api_reference_link = "https://docs.oracle.com/iaas/api/#/en/generative-ai-inference/20231130/ApplyGuardrailsResult/ApplyGuardrails"

        expected_kwargs = [
            "allow_control_chars",
            "enable_strict_url_encoding",
            "retry_strategy",
            "opc_retry_token",
            "opc_request_id",
        ]
        extra_kwargs = [_key for _key in six.iterkeys(kwargs) if _key not in expected_kwargs]
        if extra_kwargs:
            raise ValueError(
                f"apply_guardrails got unknown kwargs: {extra_kwargs!r}")

        header_params = {
            "accept": "application/json",
            "content-type": "application/json",
            "opc-retry-token": kwargs.get("opc_retry_token", missing),
            "opc-request-id": kwargs.get("opc_request_id", missing)
        }
        header_params = {k: v for (k, v) in six.iteritems(header_params) if v is not missing and v is not None}

        retry_strategy = self.async_base_client.get_preferred_retry_strategy(
            operation_retry_strategy=kwargs.get('retry_strategy'),
            client_retry_strategy=self.async_base_client.retry_strategy
        )
        if retry_strategy is None:
            retry_strategy = retry.DEFAULT_RETRY_STRATEGY

        if retry_strategy and not isinstance(retry_strategy, retry.NoneRetryStrategy):
            self.async_base_client.add_opc_retry_token_if_needed(header_params)
            self.async_base_client.add_opc_client_retries_header(header_params)

        return await self.async_base_client.call_api(
            resource_path=resource_path,
            method=method,
            header_params=header_params,
            body=apply_guardrails_details,
            response_type="ApplyGuardrailsResult",
            allow_control_chars=kwargs.get('allow_control_chars'),
            enable_strict_url_encoding=kwargs.get('enable_strict_url_encoding'),
            operation_name=operation_name,
            api_reference_link=api_reference_link,
            required_arguments=required_arguments,
            retry_strategy=retry_strategy)

    async def chat(self, chat_details, **kwargs):
        """
        Creates a response for the given conversation.


        :param oci.generative_ai_inference.models.ChatDetails chat_details: (required)
            Details of the conversation for the model to respond.

        :param str opc_retry_token: (optional)
            A token that uniquely identifies a request so it can be retried in case of a timeout or
            server error without risk of executing that same action again. Retry tokens expire after 24
            hours, but can be invalidated before that, in case of conflicting operations. For example, if a resource is deleted and purged from the system, then a retry of the original creation request
            is rejected.

        :param str opc_request_id: (optional)
            The client request ID for tracing.

        :param obj retry_strategy: (optional)
            A retry strategy to apply to this specific operation/call. This will override any retry strategy set at the client-level.

        :param bool allow_control_chars: (optional)
            allow_control_chars is a boolean to indicate whether or not this request should allow control characters in the response object.

        :param bool enable_strict_url_encoding: (optional)
            enable_strict_url_encoding is a boolean to indicate whether or not this request should enable strict url encoding in path params of a request.

        :return: A :class:`~oci.response.Response` object with data of type ChatResult
        :rtype: :class:`~oci.response.Response`
        """
        required_arguments = []
        resource_path = "/actions/chat"
        method = "POST"
        operation_name = "chat"
        api_reference_link = "https://docs.oracle.com/iaas/api/#/en/generative-ai-inference/20231130/ChatResult/Chat"

        expected_kwargs = [
            "allow_control_chars",
            "enable_strict_url_encoding",
            "retry_strategy",
            "opc_retry_token",
            "opc_request_id",
        ]
        extra_kwargs = [_key for _key in six.iterkeys(kwargs) if _key not in expected_kwargs]
        if extra_kwargs:
            raise ValueError(
                f"chat got unknown kwargs: {extra_kwargs!r}")

        header_params = {
            "accept": "application/json, text/event-stream",
            "content-type": "application/json",
            "opc-retry-token": kwargs.get("opc_retry_token", missing),
            "opc-request-id": kwargs.get("opc_request_id", missing)
        }
        header_params = {k: v for (k, v) in six.iteritems(header_params) if v is not missing and v is not None}

        retry_strategy = self.async_base_client.get_preferred_retry_strategy(
            operation_retry_strategy=kwargs.get('retry_strategy'),
            client_retry_strategy=self.async_base_client.retry_strategy
        )
        if retry_strategy is None:
            retry_strategy = retry.DEFAULT_RETRY_STRATEGY

        if retry_strategy and not isinstance(retry_strategy, retry.NoneRetryStrategy):
            self.async_base_client.add_opc_retry_token_if_needed(header_params)
            self.async_base_client.add_opc_client_retries_header(header_params)

        return await self.async_base_client.call_api(
            resource_path=resource_path,
            method=method,
            header_params=header_params,
            body=chat_details,
            response_type="ChatResult",
            allow_control_chars=kwargs.get('allow_control_chars'),
            enable_strict_url_encoding=kwargs.get('enable_strict_url_encoding'),
            operation_name=operation_name,
            api_reference_link=api_reference_link,
            required_arguments=required_arguments,
            retry_strategy=retry_strategy)

    async def embed_text(self, embed_text_details, **kwargs):
        """
        Produces embeddings for the inputs.

        An embedding is numeric representation of a piece of text. This text can be a phrase, a sentence, or one or more paragraphs. The Generative AI embedding model transforms each phrase, sentence, or paragraph that you input, into an array with 1024 numbers. You can use these embeddings for finding similarity in your input text such as finding phrases that are similar in context or category. Embeddings are mostly used for semantic searches where the search function focuses on the meaning of the text that it's searching through rather than finding results based on keywords.


        :param oci.generative_ai_inference.models.EmbedTextDetails embed_text_details: (required)
            Details for generating the embed response.

        :param str opc_retry_token: (optional)
            A token that uniquely identifies a request so it can be retried in case of a timeout or
            server error without risk of executing that same action again. Retry tokens expire after 24
            hours, but can be invalidated before that, in case of conflicting operations. For example, if a resource is deleted and purged from the system, then a retry of the original creation request
            is rejected.

        :param str opc_request_id: (optional)
            The client request ID for tracing.

        :param obj retry_strategy: (optional)
            A retry strategy to apply to this specific operation/call. This will override any retry strategy set at the client-level.

        :param bool allow_control_chars: (optional)
            allow_control_chars is a boolean to indicate whether or not this request should allow control characters in the response object.

        :param bool enable_strict_url_encoding: (optional)
            enable_strict_url_encoding is a boolean to indicate whether or not this request should enable strict url encoding in path params of a request.

        :return: A :class:`~oci.response.Response` object with data of type EmbedTextResult
        :rtype: :class:`~oci.response.Response`
        """
        required_arguments = []
        resource_path = "/actions/embedText"
        method = "POST"
        operation_name = "embed_text"
        api_reference_link = "https://docs.oracle.com/iaas/api/#/en/generative-ai-inference/20231130/EmbedTextResult/EmbedText"

        expected_kwargs = [
            "allow_control_chars",
            "enable_strict_url_encoding",
            "retry_strategy",
            "opc_retry_token",
            "opc_request_id",
        ]
        extra_kwargs = [_key for _key in six.iterkeys(kwargs) if _key not in expected_kwargs]
        if extra_kwargs:
            raise ValueError(
                f"embed_text got unknown kwargs: {extra_kwargs!r}")

        header_params = {
            "accept": "application/json",
            "content-type": "application/json",
            "opc-retry-token": kwargs.get("opc_retry_token", missing),
            "opc-request-id": kwargs.get("opc_request_id", missing)
        }
        header_params = {k: v for (k, v) in six.iteritems(header_params) if v is not missing and v is not None}

        retry_strategy = self.async_base_client.get_preferred_retry_strategy(
            operation_retry_strategy=kwargs.get('retry_strategy'),
            client_retry_strategy=self.async_base_client.retry_strategy
        )
        if retry_strategy is None:
            retry_strategy = retry.DEFAULT_RETRY_STRATEGY

        if retry_strategy and not isinstance(retry_strategy, retry.NoneRetryStrategy):
            self.async_base_client.add_opc_retry_token_if_needed(header_params)
            self.async_base_client.add_opc_client_retries_header(header_params)

        return await self.async_base_client.call_api(
            resource_path=resource_path,
            method=method,
            header_params=header_params,
            body=embed_text_details,
            response_type="EmbedTextResult",
            allow_control_chars=kwargs.get('allow_control_chars'),
            enable_strict_url_encoding=kwargs.get('enable_strict_url_encoding'),
            operation_name=operation_name,
            api_reference_link=api_reference_link,
            required_arguments=required_arguments,
            retry_strategy=retry_strategy)

    async def generate_text(self, generate_text_details, **kwargs):
        """
        Generates a text response based on the user prompt.


        :param oci.generative_ai_inference.models.GenerateTextDetails generate_text_details: (required)
            Details for generating the text response.

        :param str opc_retry_token: (optional)
            A token that uniquely identifies a request so it can be retried in case of a timeout or
            server error without risk of executing that same action again. Retry tokens expire after 24
            hours, but can be invalidated before that, in case of conflicting operations. For example, if a resource is deleted and purged from the system, then a retry of the original creation request
            is rejected.

        :param str opc_request_id: (optional)
            The client request ID for tracing.

        :param obj retry_strategy: (optional)
            A retry strategy to apply to this specific operation/call. This will override any retry strategy set at the client-level.

        :param bool allow_control_chars: (optional)
            allow_control_chars is a boolean to indicate whether or not this request should allow control characters in the response object.

        :param bool enable_strict_url_encoding: (optional)
            enable_strict_url_encoding is a boolean to indicate whether or not this request should enable strict url encoding in path params of a request.

        :return: A :class:`~oci.response.Response` object with data of type GenerateTextResult
        :rtype: :class:`~oci.response.Response`
        """
        required_arguments = []
        resource_path = "/actions/generateText"
        method = "POST"
        operation_name = "generate_text"
        api_reference_link = "https://docs.oracle.com/iaas/api/#/en/generative-ai-inference/20231130/GenerateTextResult/GenerateText"

        expected_kwargs = [
            "allow_control_chars",
            "enable_strict_url_encoding",
            "retry_strategy",
            "opc_retry_token",
            "opc_request_id",
        ]
        extra_kwargs = [_key for _key in six.iterkeys(kwargs) if _key not in expected_kwargs]
        if extra_kwargs:
            raise ValueError(
                f"generate_text got unknown kwargs: {extra_kwargs!r}")

        header_params = {
            "accept": "application/json, text/event-stream",
            "content-type": "application/json",
            "opc-retry-token": kwargs.get("opc_retry_token", missing),
            "opc-request-id": kwargs.get("opc_request_id", missing)
        }
        header_params = {k: v for (k, v) in six.iteritems(header_params) if v is not missing and v is not None}

        retry_strategy = self.async_base_client.get_preferred_retry_strategy(
            operation_retry_strategy=kwargs.get('retry_strategy'),
            client_retry_strategy=self.async_base_client.retry_strategy
        )
        if retry_strategy is None:
            retry_strategy = retry.DEFAULT_RETRY_STRATEGY

        if retry_strategy and not isinstance(retry_strategy, retry.NoneRetryStrategy):
            self.async_base_client.add_opc_retry_token_if_needed(header_params)
            self.async_base_client.add_opc_client_retries_header(header_params)

        return await self.async_base_client.call_api(
            resource_path=resource_path,
            method=method,
            header_params=header_params,
            body=generate_text_details,
            response_type="GenerateTextResult",
            allow_control_chars=kwargs.get('allow_control_chars'),
            enable_strict_url_encoding=kwargs.get('enable_strict_url_encoding'),
            operation_name=operation_name,
            api_reference_link=api_reference_link,
            required_arguments=required_arguments,
            retry_strategy=retry_strategy)

    async def list_guardrail_versions(self, opc_compartment_id, **kwargs):
        """
        List the available guardrail system versions.


        :param str opc_compartment_id: (required)
            The client compartment ID.

        :param str opc_request_id: (optional)
            The client request ID for tracing.

        :param str state: (optional)
            A filter to return only the guardrail versions whose state matches the given value.

            Allowed values are: "ACTIVE", "PREVIEW", "DEPRECATED", "RETIRED"

        :param int limit: (optional)
            For list pagination. The maximum number of results per page, or items to return in a
            paginated \"List\" call. For important details about how pagination works, see
            `List Pagination`__.

            __ https://docs.oracle.com/iaas/Content/API/Concepts/usingapi.htm#nine

        :param str page: (optional)
            For list pagination. The value of the opc-next-page response header from the previous
            \"List\" call. For important details about how pagination works, see
            `List Pagination`__.

            __ https://docs.oracle.com/iaas/Content/API/Concepts/usingapi.htm#nine

        :param obj retry_strategy: (optional)
            A retry strategy to apply to this specific operation/call. This will override any retry strategy set at the client-level.

        :param bool allow_control_chars: (optional)
            allow_control_chars is a boolean to indicate whether or not this request should allow control characters in the response object.

        :param bool enable_strict_url_encoding: (optional)
            enable_strict_url_encoding is a boolean to indicate whether or not this request should enable strict url encoding in path params of a request.

        :return: A :class:`~oci.response.Response` object with data of type GuardrailVersionCollection
        :rtype: :class:`~oci.response.Response`
        """
        required_arguments = []
        resource_path = "/guardrailVersions"
        method = "GET"
        operation_name = "list_guardrail_versions"
        api_reference_link = "https://docs.oracle.com/iaas/api/#/en/generative-ai-inference/20231130/GuardrailVersionCollection/ListGuardrailVersions"

        expected_kwargs = [
            "allow_control_chars",
            "enable_strict_url_encoding",
            "retry_strategy",
            "opc_request_id",
            "state",
            "limit",
            "page",
        ]
        extra_kwargs = [_key for _key in six.iterkeys(kwargs) if _key not in expected_kwargs]
        if extra_kwargs:
            raise ValueError(
                f"list_guardrail_versions got unknown kwargs: {extra_kwargs!r}")

        if 'state' in kwargs:
            state_allowed_values = ["ACTIVE", "PREVIEW", "DEPRECATED", "RETIRED"]
            if kwargs['state'] not in state_allowed_values:
                raise ValueError(
                    f"Invalid value for `state`, must be one of { state_allowed_values }"
                )

        query_params = {
            "state": kwargs.get("state", missing),
            "limit": kwargs.get("limit", missing),
            "page": kwargs.get("page", missing)
        }
        query_params = {k: v for (k, v) in six.iteritems(query_params) if v is not missing and v is not None}

        header_params = {
            "accept": "application/json",
            "content-type": "application/json",
            "opc-request-id": kwargs.get("opc_request_id", missing),
            "opc-compartment-id": opc_compartment_id
        }
        header_params = {k: v for (k, v) in six.iteritems(header_params) if v is not missing and v is not None}

        retry_strategy = self.async_base_client.get_preferred_retry_strategy(
            operation_retry_strategy=kwargs.get('retry_strategy'),
            client_retry_strategy=self.async_base_client.retry_strategy
        )
        if retry_strategy is None:
            retry_strategy = retry.DEFAULT_RETRY_STRATEGY

        if retry_strategy and not isinstance(retry_strategy, retry.NoneRetryStrategy):
            self.async_base_client.add_opc_client_retries_header(header_params)

        return await self.async_base_client.call_api(
            resource_path=resource_path,
            method=method,
            query_params=query_params,
            header_params=header_params,
            response_type="GuardrailVersionCollection",
            allow_control_chars=kwargs.get('allow_control_chars'),
            enable_strict_url_encoding=kwargs.get('enable_strict_url_encoding'),
            operation_name=operation_name,
            api_reference_link=api_reference_link,
            required_arguments=required_arguments,
            retry_strategy=retry_strategy)

    async def rerank_text(self, rerank_text_details, **kwargs):
        """
        Reranks the text responses based on the input documents and a prompt.

        Rerank assigns an index and a relevance score to each document, indicating which document is most related to the prompt.


        :param oci.generative_ai_inference.models.RerankTextDetails rerank_text_details: (required)
            Details required for the rerank request.

        :param str opc_retry_token: (optional)
            A token that uniquely identifies a request so it can be retried in case of a timeout or
            server error without risk of executing that same action again. Retry tokens expire after 24
            hours, but can be invalidated before that, in case of conflicting operations. For example, if a resource is deleted and purged from the system, then a retry of the original creation request
            is rejected.

        :param str opc_request_id: (optional)
            The client request ID for tracing.

        :param obj retry_strategy: (optional)
            A retry strategy to apply to this specific operation/call. This will override any retry strategy set at the client-level.

        :param bool allow_control_chars: (optional)
            allow_control_chars is a boolean to indicate whether or not this request should allow control characters in the response object.

        :param bool enable_strict_url_encoding: (optional)
            enable_strict_url_encoding is a boolean to indicate whether or not this request should enable strict url encoding in path params of a request.

        :return: A :class:`~oci.response.Response` object with data of type RerankTextResult
        :rtype: :class:`~oci.response.Response`
        """
        required_arguments = []
        resource_path = "/actions/rerankText"
        method = "POST"
        operation_name = "rerank_text"
        api_reference_link = "https://docs.oracle.com/iaas/api/#/en/generative-ai-inference/20231130/RerankTextResult/RerankText"

        expected_kwargs = [
            "allow_control_chars",
            "enable_strict_url_encoding",
            "retry_strategy",
            "opc_retry_token",
            "opc_request_id",
        ]
        extra_kwargs = [_key for _key in six.iterkeys(kwargs) if _key not in expected_kwargs]
        if extra_kwargs:
            raise ValueError(
                f"rerank_text got unknown kwargs: {extra_kwargs!r}")

        header_params = {
            "accept": "application/json",
            "content-type": "application/json",
            "opc-retry-token": kwargs.get("opc_retry_token", missing),
            "opc-request-id": kwargs.get("opc_request_id", missing)
        }
        header_params = {k: v for (k, v) in six.iteritems(header_params) if v is not missing and v is not None}

        retry_strategy = self.async_base_client.get_preferred_retry_strategy(
            operation_retry_strategy=kwargs.get('retry_strategy'),
            client_retry_strategy=self.async_base_client.retry_strategy
        )
        if retry_strategy is None:
            retry_strategy = retry.DEFAULT_RETRY_STRATEGY

        if retry_strategy and not isinstance(retry_strategy, retry.NoneRetryStrategy):
            self.async_base_client.add_opc_retry_token_if_needed(header_params)
            self.async_base_client.add_opc_client_retries_header(header_params)

        return await self.async_base_client.call_api(
            resource_path=resource_path,
            method=method,
            header_params=header_params,
            body=rerank_text_details,
            response_type="RerankTextResult",
            allow_control_chars=kwargs.get('allow_control_chars'),
            enable_strict_url_encoding=kwargs.get('enable_strict_url_encoding'),
            operation_name=operation_name,
            api_reference_link=api_reference_link,
            required_arguments=required_arguments,
            retry_strategy=retry_strategy)

    async def summarize_text(self, summarize_text_details, **kwargs):
        """
        Summarizes the input text.


        :param oci.generative_ai_inference.models.SummarizeTextDetails summarize_text_details: (required)
            Details for summarizing the text.

        :param str opc_retry_token: (optional)
            A token that uniquely identifies a request so it can be retried in case of a timeout or
            server error without risk of executing that same action again. Retry tokens expire after 24
            hours, but can be invalidated before that, in case of conflicting operations. For example, if a resource is deleted and purged from the system, then a retry of the original creation request
            is rejected.

        :param str opc_request_id: (optional)
            The client request ID for tracing.

        :param obj retry_strategy: (optional)
            A retry strategy to apply to this specific operation/call. This will override any retry strategy set at the client-level.

        :param bool allow_control_chars: (optional)
            allow_control_chars is a boolean to indicate whether or not this request should allow control characters in the response object.

        :param bool enable_strict_url_encoding: (optional)
            enable_strict_url_encoding is a boolean to indicate whether or not this request should enable strict url encoding in path params of a request.

        :return: A :class:`~oci.response.Response` object with data of type SummarizeTextResult
        :rtype: :class:`~oci.response.Response`
        """
        required_arguments = []
        resource_path = "/actions/summarizeText"
        method = "POST"
        operation_name = "summarize_text"
        api_reference_link = "https://docs.oracle.com/iaas/api/#/en/generative-ai-inference/20231130/SummarizeTextResult/SummarizeText"

        expected_kwargs = [
            "allow_control_chars",
            "enable_strict_url_encoding",
            "retry_strategy",
            "opc_retry_token",
            "opc_request_id",
        ]
        extra_kwargs = [_key for _key in six.iterkeys(kwargs) if _key not in expected_kwargs]
        if extra_kwargs:
            raise ValueError(
                f"summarize_text got unknown kwargs: {extra_kwargs!r}")

        header_params = {
            "accept": "application/json",
            "content-type": "application/json",
            "opc-retry-token": kwargs.get("opc_retry_token", missing),
            "opc-request-id": kwargs.get("opc_request_id", missing)
        }
        header_params = {k: v for (k, v) in six.iteritems(header_params) if v is not missing and v is not None}

        retry_strategy = self.async_base_client.get_preferred_retry_strategy(
            operation_retry_strategy=kwargs.get('retry_strategy'),
            client_retry_strategy=self.async_base_client.retry_strategy
        )
        if retry_strategy is None:
            retry_strategy = retry.DEFAULT_RETRY_STRATEGY

        if retry_strategy and not isinstance(retry_strategy, retry.NoneRetryStrategy):
            self.async_base_client.add_opc_retry_token_if_needed(header_params)
            self.async_base_client.add_opc_client_retries_header(header_params)

        return await self.async_base_client.call_api(
            resource_path=resource_path,
            method=method,
            header_params=header_params,
            body=summarize_text_details,
            response_type="SummarizeTextResult",
            allow_control_chars=kwargs.get('allow_control_chars'),
            enable_strict_url_encoding=kwargs.get('enable_strict_url_encoding'),
            operation_name=operation_name,
            api_reference_link=api_reference_link,
            required_arguments=required_arguments,
            retry_strategy=retry_strategy)

    async def close(self):
        """Closes the underlying asynchronous HTTP session."""
        await self.async_base_client.close()

    async def __aenter__(self):
        return self

    async def __aexit__(self, exc_type, exc_value, traceback):
        await self.close()
